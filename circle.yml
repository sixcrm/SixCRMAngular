general:
  branches:
    only:
      - Auth0

machine:
  timezone:
    America/Los_Angeles
  node:
    version: 7.6.0

dependencies:
  pre:
    - aws configure set preview.cloudfront true

test:
  override:
    - "true"

deployment:
  development:
    branch: Auth0
    commands:
      - ./build.sh
